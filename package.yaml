spec-version:                       0.31.0
name:                               reacss
version:                            0.1.0.0
# synopsis:
# description:
category:                           Web
homepage:                           https://github.com/githubuser/reacss#readme
bug-reports:                        https://github.com/githubuser/reacss/issues
author:
  - Jaewon Seo
  - Junyoung Clare Jang
maintainer:
  - Jaewon Seo
  - Junyoung Clare Jang
copyright:                          2020 JaewonSeo, Junyoung Clare Jang
license:                            BSD3
license-file:                       LICENSE
# tested-with:
# build-type:
extra-source-files:
  - README.md
# extra-doc-files:
# data-files:
# data-dir:
github:                             Reacss/Reacss
# custom-setup:
  # dependencies:

# flags:

library:
  ## Common fields
  source-dirs:
    - src
  dependencies:
    - megaparsec >= 8.0.0 && < 9
  ## Library fields
  # exposed:
  # exposed-modules:
  # generated-exposed-modules:
  # other-modules:
  # generated-other-modules:
  # reexported-modules:
  # signatures:

# internal-libraries:

executables:
  reacss:
    ## Common fields
    source-dirs:
      - app
    ghc-options:                    -threaded -rtsopts -with-rtsopts=-N
    dependencies:
      - reacss
    ## Executable fields
    main:                           Main.hs
    # other-modules:
    # generated-other-modules:

# executable:

tests:
  reacss-test:
    ## Common fields
    source-dirs:
      - test
    ghc-options:                    -threaded -rtsopts -with-rtsopts=-N
    dependencies:
      - reacss
    ## Test fields
    main:                           Spec.hs
    # other-modules:
    # generated-other-modules:

# benchmarks:

# defaults:
  # github:
  # ref:
  # path:
  # local:

## Common fields
# buildable:
# source-dirs:
# default-extensions:
# other-extensions:
# ghc-options:
# ghc-prof-options:
# ghcjs-options:
# cpp-options:
# cc-options:
# c-sources:
# cxx-options:
# cxx-sources:
# js-sources:
# extra-lib-dirs:
# extra-libraries:
# include-dirs:
# install-includes:
# frameworks:
# extra-frameworks-dirs:
# ld-options:
dependencies:
  - base >= 4.13 && < 5
# pkg-config-dependencies:
# build-tools:
# system-build-tools:
# when:
